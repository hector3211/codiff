# codiff Local TODO

- [ ] Add line-level comment badges in the diff gutter
- [ ] Add "Send to AI" action on comments with command preview
- [ ] Add command status timeline (queued/running/succeeded/failed)
- [x] Test change: verify live diff updates on TODO edits
- [ ] Add apply-mode toggle persistence (manual vs auto)
- [ ] Add "Past Sessions" panel with click-to-load snapshots
- [ ] Add unit tests for DB comment/session operations
- [ ] Add git-HEAD baseline mode alongside session-start baseline
- [ ] Add configurable ignore patterns (respect `.gitignore` + custom excludes)
- [ ] Add cancel/retry controls for running and failed AI commands
- [ ] Add unit tests for watcher debounce/coalescing behavior
- [ ] Add persisted UI filters (recent-only, resolved comments visibility)
- [ ] Add keyboard shortcuts cheat sheet modal (`?` to open)
- [ ] Add inline per-file search/filter in Changed Files panel
- [ ] Add empty-state guidance with quick start actions in the UI
  <!-- codiff test marker updated -->
- [ ] Add export session review as markdown report
- [ ] Add integration test coverage for `/api/state` and `/events`
- [ ] review

## Notes

- Keep this file local-only (ignored by git).
- Use it for scratch planning during rapid iteration.
